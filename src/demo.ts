/**
 * デモモード（公開デモ用のサンプル出力）
 *
 * 本ツールは BYOK（利用者が自分のAPIキーを持ち込む）方式なので、
 * 公開してもAPIコストは利用者側にかかる。ただしキーが無い人は何も体験できない。
 * そこで「サンプル出力を見る」だけの経路を用意し、キー無しでも媒体別の
 * 書き分けが伝わるようにする。生成は行わず、事前に作った出力を再生するだけ。
 *
 * 有効化: VITE_DEMO_MODE=1
 */
export const DEMO_MODE = import.meta.env.VITE_DEMO_MODE === '1'

/** サンプル出力に使う共通トピック */
export const SAMPLE_TOPIC =
  'Cloudflare Pages にデプロイした Next.js で next/image の最適化が効かなかった話'

/**
 * 媒体別のサンプル出力。
 * 同じトピックでも媒体ごとに構成・語調・長さが変わることを見せるのが目的。
 */
export const SAMPLE_OUTPUTS: Record<'zenn' | 'qiita' | 'note' | 'x', string> = {
  zenn: `---
title: "Cloudflare Pages で next/image の最適化が効かず、画像だけ重いままだった"
emoji: "🖼️"
type: "tech"
topics: ["nextjs", "cloudflare", "画像最適化"]
published: false
---

## 何が起きたか

ポートフォリオを Next.js で作り、Cloudflare Pages にデプロイしました。ローカルでは \`next/image\` が WebP を返していたのに、本番では元の PNG がそのまま配信されていました。Lighthouse の画像スコアだけが伸びない。

## 原因

\`next/image\` の最適化は Next.js のサーバー（または Vercel の Image Optimization）が担っています。Cloudflare Pages に \`@cloudflare/next-on-pages\` で載せると、この最適化エンドポイントが動きません。つまり「最適化されない \`<img>\`」として出力されます。

## どうしたか

ビルド前に画像を WebP へ変換して \`public/\` に置き、\`next/image\` には最適化済みの実体を渡すようにしました。

\`\`\`bash
# 事前変換（例）
npx @squoosh/cli --webp auto public/images/*.png
\`\`\`

\`unoptimized\` を付けて明示するのも手ですが、**配信されるファイル自体を軽くしないと意味がない**ので、変換は必須です。

## 学び

「フレームワークの機能」と「ホスティングが提供する機能」を分けて考えていませんでした。\`next/image\` は前者に見えて、実体は後者に依存します。ホスティングを変えるときは、**フレームワーク側の便利機能がどこで実行されているか**を一度たどるべきでした。`,

  qiita: `# Cloudflare Pages では next/image の最適化が動かない（対処法つき）

## 結論

**Cloudflare Pages に \`@cloudflare/next-on-pages\` で Next.js を載せると、\`next/image\` の画像最適化は動きません。** 画像は事前に WebP 化して配置してください。

## なぜ動かないのか

\`next/image\` の最適化は、リクエスト時に Next.js のサーバー側で行われます。具体的には \`/_next/image?url=...&w=...&q=...\` というエンドポイントが画像を変換して返す仕組みです。

Cloudflare Pages（Workers ランタイム）ではこの最適化エンドポイントが提供されないため、\`next/image\` は変換を伴わない \`<img>\` として振る舞います。**ローカルの \`next dev\` では動くので気づきにくい**のが厄介です。

## 対処法

### 1. 事前に WebP 化する（推奨）

\`\`\`bash
npx @squoosh/cli --webp auto public/images/*.png
\`\`\`

### 2. \`unoptimized\` を明示する

\`\`\`tsx
<Image src="/images/hero.webp" alt="" width={1200} height={630} unoptimized />
\`\`\`

これは「最適化しない」ことを宣言するだけで、**ファイルが軽くなるわけではありません**。1 とセットで使ってください。

### 3. Cloudflare Images を使う

有料。枚数が多く、動的にリサイズしたい場合はこちら。

## 確認方法

デプロイ後、DevTools の Network タブで画像のレスポンスを見ます。\`content-type: image/png\` のままなら最適化されていません。

## まとめにかえて

フレームワークの機能とホスティングの機能は別物です。移行時は「この機能はどこで実行されているか」を確認してください。`,

  note: `## 「動いているのに、遅い」を放置していた

ポートフォリオサイトを作り直しました。デプロイも通ったし、見た目も整った。それで満足していたんです。

ある日、スマホで自分のサイトを開いたら、画像がじわっと出てきました。あれ、と思って計測すると、画像だけが妙に重い。

原因は、フレームワークの便利機能が、引っ越し先では動いていなかったことでした。手元では動く。本番では動かない。しかもエラーは出ない。ただ静かに、最適化されていない画像が配信され続けていた。

これは技術の話というより、**「動いた」で確認をやめる癖**の話だと思います。デプロイが通ったことと、意図した通りに動いていることは、別のことでした。

直し方自体は単純で、あらかじめ画像を軽くしてから置くだけです。10分で終わりました。10分で終わることを、数週間放置していたわけです。

作ったものを人に見せる前に、一度だけでいいから、自分がユーザーとして触ってみる。当たり前のことを、当たり前にやろうと思いました。`,

  x: `Next.js を Cloudflare Pages に載せたら next/image の最適化が効いてなかった

・ローカル(next dev)では動く
・本番では変換されず元のPNGがそのまま配信される
・エラーは出ない。ただ静かに重い

最適化はNext.jsのサーバー側で走る機能なので、Workersランタイムでは提供されない

対処：ビルド前にWebP化して public/ に置く
unoptimized を付けるのは「宣言」であって軽くはならない

フレームワークの機能とホスティングの機能を分けて考えていなかった、という話でした`,
}
